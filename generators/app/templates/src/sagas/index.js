// Be sure to include sagas in store/configureStore.*

/*
export function* incrementAsync() {
  yield call(delay, 1000);
  yield put({ type: 'INCR' });
}

export default function* exampleSaga() {
  yield takeEvery('INCREMENT_ASYNC', incrementAsync);
}
*/
